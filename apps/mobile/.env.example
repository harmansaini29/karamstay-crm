# Point this at your deployed backend (e.g. https://api.karamstay.com/api/v1) in prod.
EXPO_PUBLIC_API_BASE_URL=http://localhost:8000/api/v1
# "true" ONLY for offline local-dev against the in-memory mock; leave "false" otherwise.
EXPO_PUBLIC_MOCK_API=false
